import styled, { css } from "styled-components";

function Header(){
    return(
        <TopNavigation>
            헤더
        </TopNavigation>
    )
}

const TopNavigation = styled.div`
    width: 100%;
`;

export default Header;